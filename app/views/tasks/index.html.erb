<h1>タスクリスト一覧</h1>

<ul>
  <% @tasks.each do |tasks| %>
    <li><%= link_to tasks.id, tasks %> : <%= tasks.status %> &gt; <%= tasks.content %> </li>
  <% end %>
</ul>
<%= paginate @tasks %>

<%= link_to '新規タスクの作成', new_task_path %>